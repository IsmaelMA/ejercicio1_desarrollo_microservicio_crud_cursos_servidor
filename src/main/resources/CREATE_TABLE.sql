CREATE TABLE
    `bd_cursos_cuarta_semana`.`cursos` (
        `cod_curso` VARCHAR(10) NOT NULL,
        `nombre` VARCHAR(45) NULL,
        `duracion` INT NULL,
        `precio` INT NULL,
        PRIMARY KEY (`cod_curso`)
    );